{
  "name": "sws-webconsole",
  "version": "0.1.0",
  "description": "Web console for SWS (Sound Wav Script)",
  "private": true,
  "scripts": {
    "start": "npx http-server ./web -p 8000 -c-1",
    "lint": "eslint ./web/static || true",
    "test": "echo \"No frontend tests defined\" && exit 0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=16"
  }
}
